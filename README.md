# AI Hominem

Dependency-free MVP for the live debate coach spec.

```sh
node server.js
```

Open `http://localhost:4173`, then use `Start` for browser speech recognition or `Demo` for the canned AI-in-education debate.

Skipped: Deepgram and LLM API wiring. Add when API keys are available and the demo needs real streaming services.
